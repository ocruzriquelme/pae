<%= simple_form_for @pregunta, url: preguntas_path, as: 'pregunta'  do |f| %>
    <%= f.error_notification %>

    <p>
      <%= f.input :pregunta%>
    </p>

    <%= f.button :submit, "Guardar", class: "my-button" %>
<% end %>